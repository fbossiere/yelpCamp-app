<% include partials/header %>
<div class='container'>
    <header class='jumbotron'>
        <h1>Welcome to YelpCamp!</h1>
        <p>View our hand-picked campgrounds from all over the world</p>
        <p><a class='btn btn-primary' href='/campgrounds/new'><i class="fa fa-plus-circle" aria-hidden="true"></i> Add New Campground</a></p>
    </header>
    
    <h3>Our Most Popular Sites</h3>
    <div class='row campgrounds text-center'>
        <% campgrounds.forEach(function(campground){ %>
            <div class='campground col-md-3 col-sm-6'>
                
                <div class='thumbnail'>
                    <div class='caption'>
                        <h4><%= campground.name %></h4>
                    </div>
                    <img src="<%= campground.imageURL %>">
                    <a class='btn btn-primary' href='/campgrounds/<%= campground._id %>'>More Info</a>
                </div>
            </div>
        <% }); %>
    </div>
    
<% include partials/footer %>